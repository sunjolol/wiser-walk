# Critique, round one. Verify each finding in a capture before acting on it: critics can be wrong.

## Verdict
No. He would open /r/theology-compass/01IBS6/ first — the page he called the bar — and find it is the only page with no band, no ghost, no tiles and no section heads, while /axis/ renders the same content far better. That alone reads as the pass being aimed at everything except the thing he pointed at. Then /c/, where the rails are #E8E8E8 on #EEE and simply invisible; /method/, where 4,000px of 89 identical grey pills expose raw keys like `axis:grace:bands`; the article page, with 1,900px of empty black column; and /play/, which has no sheet, no header and a 520px hole at desktop. The pages that do hold up — /, /quizzes/, /q/, /axis/, /tradition/ headers — are genuinely close to the references. The rest still fall short of both the home page and the result page's instruments.

- [ugly] /r/theology-compass/01IBS6/ (desktop 1360 and phone 390, light and dark)
  WHAT: The result page — the page the brief names as the bar — never got the pass. It is the only content page with NO band: no dark eyebrow chip, no ghost word, no tiles, no wash header. It opens with a bare 12px blue '◎ THEOLOGY COMPASS' label sitting on the plain sheet. Its section headings ('Loose neighbours, not matches', 'Where these differences come from') are plain bold serif h2s with no kicker and no gradient rule, while the axis page renders the SAME content (two cases, passages, timeline, further reading) with SectionHeads, a dark panel, a rotated gradient strip and dotted spec rules. Further reading is still bulleted with • dots; on /axis/ the identical list has dotted rules. The reference page is now the least designed page on the site, and the owner will open it first.
  FIX: Give it the Hero band (eyebrow 'THEOLOGY COMPASS', ghost 'you', tiles from real data: 6 axes / 18 traditions / n% closest), convert every h2 to <SectionHead>, and reuse the axis page's panel treatments for passages, timeline and further reading so the two pages read as one system.
  WHERE: site/src/pages/r/[quiz]/[code].astro (no <Hero>/<SectionHead> used; compare site/src/pages/axis/[quiz]/[axis].astro which does)
- [broken] /c/theology-compass/01IBS6.01IBS6/ (desktop 1360 and phone 390, light (the default))
  WHAT: The compare rails are invisible. I sampled the capture: the track is rgb(232,232,232) on a rgb(241,241,241) panel — about 1.05:1. With both codes identical there is no fill anywhere, so all six axes render as a hairline you cannot see plus one small ring floating in white space. The whole 'side by side' panel reads as a widget that failed to load. The same track colour also kills the 'no lean' rows on /r/ (Grace, Worship) and the centrist rows on /tradition/reformed-baptist-1689/.
  FIX: Darken the track inside light panels (e.g. a --rail-track token around #D6D6D6, or an inset hairline border), and when two results coincide draw one marker with an explicit 'both here' label instead of two stacked rings.
  WHERE: site/src/styles/site.css:454 — .rail-track { background: var(--shell) } where --shell (#E8E8E8) sits on --panel (#EEEEEE). site.css is on the do-not-touch list, so override in site/src/styles/pages/reference.css.
- [ugly] /tradition/eastern-orthodox/ and /r/theology-compass/01IBS6/ (desktop 1360, light)
  WHAT: Every nearest-neighbour bar has a hard vertical colour seam at the identical x (I measured a 208-point RGB jump at x=601 on the 91% Catholic bar and 90-point jumps at the same x=601 on the 66% and 65% bars, whose bars end at 1085, 837 and 829). The gradient is a fixed 416px tile that repeats, so a long bar wraps from orange straight back to blue mid-bar. It looks like a rendering fault, and the seam sits at the same pixel on every row so it reads as a tick mark that means nothing. On the lead row the floral overlay adds four more seams, and the orange '91% MATCH · CLOSEST' sits directly on peach.
  FIX: background-size: 100% 100% with background-repeat: no-repeat so the gradient spans each bar's own width; drop the floral from the lead row or move the label off the saturated zone onto the card's plain tail.
  WHERE: site/src/styles/site.css:700 — .outcome-fill { background-image: var(--grad-r); background-size: 26rem 100% } (repeat is implied)
- [ugly] /articles/what-does-it-mean-to-be-christlike/ (desktop 1360, light and dark)
  WHAT: The dark left column is roughly 1,900px of completely empty charcoal. Its content (ARTICLE / PUBLISHED / READING TIME / TOPICS / the five-item TOC) ends at y≈920; the next thing in it is an 'ALL ARTICLES' button pinned at the very bottom around y≈2,800, with a clipped ghost fragment colliding with the button. ref-riche-sheet's charcoal column is packed top to bottom with stat blocks; this is a black wall beside the text. It is also duplicated: 'ALL ARTICLES' appears again as an outline button ~300px below the 'Keep going.' panel. In dark theme the column's fill is close enough to the page that it stops reading as a panel at all, so it is just a void with a coloured left rule.
  FIX: Make the aside position: sticky and height: fit-content so it hugs its content, or fill it the way the reference does — reading progress, the topic chips, a related-article stripe card, the compass CTA. Delete one of the two 'ALL ARTICLES' buttons.
  WHERE: site/src/pages/articles/[slug].astro (the aside), site/src/styles/pages/prose.css
- [ugly] /method/ (desktop 1360, light and dark)
  WHAT: The changelog is about 4,000 vertical pixels of 89 identical #EEE pills on #E8E8E8 with no dark panel, no tiles, no rhythm and no variation — the single most undesigned region on the site and exactly the 'plain text page' the owner rejected. It also exposes raw internal data keys to readers as the row titles ('axis:grace:bands', 'axis:kingdom:readmore', 'axis:tradition:passages'), and the summaries truncate mid-clause with an ellipsis ("('Effectual grace' excludes Lutheran…"). The group filter chips wrap with a single orphan 'OTHER 2' on a second row, and the '89 CHANGES / 6 GROUPS' tiles are stacked vertically in a narrow right column instead of the kit's horizontal tile row.
  FIX: Render each group as its own region with a SectionHead and a dark group header; translate the keys to human labels ('Grace — band wording'); truncate on a word boundary with no dangling quote; put the counts in a horizontal <ul class="tiles">; collapse groups by default so the page is scannable.
  WHERE: site/src/pages/method.astro, site/src/styles/pages/reference.css
- [ugly] /play/sounds-like-scripture/ (desktop 1360, light)
  WHAT: The one page that does not look like the site at all. No sheet frame, no Wiser Walk header, no footer — a 450px column marooned in the middle of 1360px with about 450px of bare striped background on each side, and a ~520px empty grey gap between the 'HOW A RUN GOES' panel and the PLAY button, because a phone flex layout is being stretched to desktop. The primary CTA is the weakest button on the site: 'PLAY' in mid-grey on a washed-out gradient (I sampled label rgb(203,199,200) against fill rgb(202,203,207) at the sample point), where the home page's equivalent uses bold charcoal on a saturated gradient. 'TODAY'S TEN' is pale blue on pale grey.
  FIX: Cap the column at ~560px but centre it inside the sheet frame with the site header and a minimal footer; replace the min-height:100vh flex push with normal flow so the CTAs sit under the panel; give PLAY the home page's button treatment (charcoal bold on the full-strength gradient) and show the canon buttons as a required choice rather than silently disabling PLAY.
  WHERE: site/src/pages/play/sounds-like-scripture.astro, site/src/styles/pages/play.css
- [ugly] /axis/theology-compass/kingdom/ (also /about/, /method/, /games/, /articles/, /tradition/reformed-baptist-1689/) (desktop 1360 and tablet 768, light)
  WHAT: The band's ghost word collides with the tile row. On /axis/kingdom/ the giant 'kingdom' runs straight behind '3 STATEMENTS / 10 MOMENTS / 9 PASSAGES / 18 TRADITIONS' — the dot of the 'i' pokes out between the PASSAGES and TRADITIONS tiles like a stray blob and the 'k' ascender cuts between two tiles. Same on /tradition/reformed-baptist-1689/ ('reformed' under the tiles), /method/ ('method' under DISPUTES), /about/, /games/ and /articles/ at 768. On /axis/table/ the ghost happens to start clear of the tiles, so the placement is uncontrolled rather than composed.
  FIX: Anchor the ghost to the band's bottom-right with a right-of-tiles inset (or clamp its size by word length) so it always sits in the empty right half, as it does on the home page reference and on /axis/table/.
  WHERE: the .band-ghost placement shared by the Hero band, site/src/styles/kit.css and the per-page overrides in site/src/styles/pages/reference.css
- [ugly] /articles/what-does-it-mean-to-be-christlike/ (desktop 1360 and tablet 768, light)
  WHAT: The band ghost 'christlike' runs directly through the lede paragraph: the 'h' ascender and the 'i' dot cut through 'and not a set of manners —', and the whole three-line lede sits on a mix of white ghost strokes and the peach wash. It is also the only ghost word on the site set in title case ('Christlike'); every other page uses one lowercase word, which the brief requires.
  FIX: Lowercase the ghost, push its baseline below the lede (or give the band the same height as /axis/ so the ghost clears the copy), and add a tile row so the band matches its siblings.
  WHERE: site/src/pages/articles/[slug].astro (the ghost value and band height), site/src/styles/pages/prose.css
- [ugly] /about/ (desktop 1360, light and dark)
  WHAT: Two panels are split into two columns where the second column is nearly empty. In 'what this is' the left column runs seven lines and the right column is two lines followed by ~200px of blank panel. In 'on fairness' the left runs six lines and the right three, leaving ~100px of dead dark panel. It reads as a CSS `columns` rule applied to a two-paragraph body rather than a composed layout. Below it, 'what this is not' puts five cards into a 3+2 grid with an empty sixth cell, and the cards are wildly unequal — 'Not a church.' is one line with ~80px of blank beneath it next to two four-line cards.
  FIX: Use the second column for something real — a spec list, the tiles, or the quizzes/games/articles stripe cards — or drop to one measure. Balance 'what this is not' to a 2-up or give the fifth card the full width as a wide closing statement.
  WHERE: site/src/pages/about.astro, site/src/styles/pages/prose.css
- [ugly] /axis/theology-compass/table/ (desktop 1360, light)
  WHAT: Three craft failures in the lower half. (1) 'the passages' panel: the ten chips fill only the top half and about 120px of empty panel sits below them, while the rotated gradient label strip runs the full panel height past its own content. (2) The 'Where do you land?' dark CTA panel is 45% void — the copy and buttons occupy the left 55% and the right half holds only a clipped ghost. (3) The prev/next pair below it is two flat #EEE boxes on the #E8E8E8 sheet with essentially no visible edge, and directly under them 'Suggest a correction. Corrections that are applied are recorded in the public changelog.' hangs as an unstyled orphan paragraph on the bare page — the exact 'plain text page' look the brief was written to kill.
  FIX: Shrink the passages panel to its content (or put the opened verse alongside), put a spec list or an instrument in the empty half of the dark panel the way /quizzes/ does with SCORED/STORED/RESULT LINK, style prev/next as stripe cards with the axis pole names, and wrap the correction line in a small dark chip-led note.
  WHERE: site/src/pages/axis/[quiz]/[axis].astro, site/src/styles/pages/reference.css
- [ugly] /tradition/eastern-orthodox/ and /tradition/reformed-baptist-1689/ (desktop 1360 and phone 390, light)
  WHAT: The hatched 41–59 band is painted on top of the coloured fill, so each rail shows a striped chunk in the middle of an otherwise solid bar — on GRACE the hatch is grey to the left of centre and orange to the right of it, on TABLE the blue fill turns striped where it crosses. It reads as a rendering artifact, and a reader cannot tell whether the stripes say something about their score. Separately, the axis name is always blue no matter which pole the sketch leans to (GRACE is blue while its fill and knob are orange), so the one place colour is used semantically contradicts itself. The rails also sit on a strong photographic wash — the blue head silhouette is clearly legible through them — which further eats the hairline track.
  FIX: Draw the zone under the fill (lower z-index) or as an outline only; colour the axis label to match the lean, or make it neutral ink on every row; drop the wash opacity on any panel that contains rails.
  WHERE: site/src/components/RailStack.astro and .rail-zone in site/src/styles/site.css:459; the wash is on the panel in site/src/styles/pages/reference.css
- [weak] /about/, /articles/, /quizzes/, /games/ (the Card component) (desktop 1360 and tablet 768, light)
  WHAT: The card's action link is not pinned to the card bottom, so every multi-card row is ragged. On /about/ 'ALL QUIZZES →' sits ~50px above 'ALL GAMES →' and 'ALL ARTICLES →', and the vertical tabs '1 QUIZ' / '1 GAME' / '3 PIECES' end at three different heights in the same row. On /articles/ the middle card's 'READ →' drops 24px below the other two. Each short card ends with an obvious dead patch.
  FIX: Make the card a flex column with the meta/go block margin-top:auto so every CTA and every vertical tab lands on the same baseline across a row.
  WHERE: site/src/components/Card.astro / QuizPreview.astro and the .card rules in site/src/styles/kit.css
- [weak] /method/, /about/, /articles/, /games/, /axis/* (desktop 1360, light and dark)
  WHAT: The site now has at least three different dark surfaces and they do not agree. Measured from the captures in light: rgb(38,38,38) on the AxisMap panel, the home feature art and the footer; rgb(46,46,46) on the /quizzes/ 'Nothing you answer leaves your browser' panel; rgb(42,42,51) — visibly blue-tinted against a neutral charcoal palette — on /about/ 'on fairness', /articles/ 'Or see where you stand' and the article 'Keep going.' panel. In dark theme it is worse: on /method/ the dark panel measured rgb(27,27,33) against a page of rgb(16,16,21), roughly 1.35:1, so the dark panels — the site's main contrast device — simply vanish and the page loses all rhythm. The dark-theme value is set by a hard-coded literal, which also breaks the brief's tokens-only rule.
  FIX: One --dark-fill token for every dark surface (panel, feature art, footer, chips), remove the literal at kit.css:185, and in dark theme raise it far enough above the page (or add a hairline border) that a dark panel is still readable as a panel.
  WHERE: site/src/styles/kit.css:185 — :root[data-theme="dark"] .dark { background-color: #2A2A33 } overriding --dark-fill (#1C1C23 light / #343441 dark, site.css:77 and :118)
- [ugly] /c/theology-compass/01IBS6.01IBS6/ (desktop 1360 and phone 390, light)
  WHAT: The legend does not describe what is drawn. It says first = solid dark dot, second = dark ring, but the markers on the rails are coloured by pole: orange on Table, Gifts and Kingdom, blue on Authority, dark on Grace and Worship. So the legend's colour code is contradicted by every row. With two identical codes both readers land on the same point and only one ring appears, with nothing saying two markers are stacked — a reader sees six lone rings and no gap and cannot tell whether the page worked. The 'same band: …' readouts are small italic grey right-aligned over a strong wash. At 390 the AUTHORITY row wraps its readout to a second line so that row is taller than the other five, breaking the vertical rhythm.
  FIX: Give each reader a fixed identity (shape or fill, not pole colour) and make the legend match; when two markers coincide, draw one badge and label it; reserve a fixed two-line height for the readout so rows stay on a grid.
  WHERE: site/src/pages/c/[quiz]/[codes].astro and site/src/components/RailOverlay.astro
- [weak] / (desktop 1360 and tablet 768, light and dark)
  WHAT: Every band on the site uses the identical wash silhouette image at the identical position and crop — home, /quizzes/, /q/, /axis/, /tradition/, /articles/, /about/, /method/, /games/, /c/. I compared the same 260px right-edge strip across seven pages and they are pixel-alike. Only the title, chip and ghost word change. Family resemblance has become sameness: nine page headers that look like one template filled in nine times, which is close to the complaint that started this pass.
  FIX: Vary the wash per section — hue rotation, flip, crop offset or scale keyed off the page group (quiz / reference / read / play) — so a reader can tell where they are from the header alone while the palette stays locked.
  WHERE: the shared band background in site/src/styles/kit.css (.band / --band-wash)
- [weak] /method/ (phone 390, light)
  WHAT: The 'ON THIS PAGE' jump bar shows only three of its eight chips; '03 WHAT CHANGED' is cut hard at the viewport edge and chips 04–08 are off-screen. The strip is overflow-x: auto so it does scroll, but there is no fade mask, no scrollbar, no counter and no panel around it — the only hint that more exists is a chip sliced in half. Five of eight sections of the longest page on the site are effectively hidden on the device the brief says is priority one.
  FIX: Add a right-edge gradient mask and scroll-snap, or wrap the chips at 390 into a two-row grid — eight short chips fit in two rows at that width.
  WHERE: site/src/components/JumpBar.astro, site/src/styles/pages/reference.css (nav.jump ul)
- [weak] /quizzes/ (desktop 1360, light)
  WHAT: Two problems. The flagship block is a verbatim repeat of the home page's quiz feature — same dark half, same compass, same 'THE FLAGSHIP · AUDITED' tab, same spec list, same six pole chips — so the quizzes index adds nothing to a reader who has just scrolled the home page. And the 'in draft' section below it puts a single ~493px stripe card in a 1000px column, leaving 500px of bare grey to its right under a full-size section head.
  FIX: Give the index a different cut of the same data — e.g. the six axes as a chip grid, the audit tiles, or the compass at full width — and either widen the draft card to the column or pair it with a 'what's coming' panel.
  WHERE: site/src/pages/quizzes.astro (shares QuizPreview.astro with index.astro)
- [weak] /articles/ and /c/theology-compass/01IBS6.01IBS6/ (desktop 1360, light)
  WHAT: Section heads applied to content too small to carry them. On /articles/, 'WHAT THE 3 PIECES ARE ABOUT / topics' introduces four tiny chips that occupy a third of the width, leaving two thirds empty — the heading is physically larger than its content. On /c/, 'THE FULL RESULT BEHIND EACH CODE / each on its own' introduces two outline buttons. Both read as a template being filled for its own sake.
  FIX: Fold the topics chips into the 'everything here' head as a filter row, and make the two result links a pair of stripe cards showing each reader's headline so the section earns its head.
  WHERE: site/src/pages/articles/index.astro, site/src/pages/c/[quiz]/[codes].astro
- [weak] /games/ and / (desktop 1360, light and dark)
  WHAT: The game's art is a grey wireframe: a dark card with three flat grey bars where the line would be, under a quote mark. On a 1360 flagship feature it reads as a skeleton that never finished loading, not as an illustration, and it is the only thing representing the game on two pages. In the dark half the ghost word is clipped at both ends and renders as 'ripture?' floating in the corner. Separately the /games/ h1 is entirely blue, breaking the 'charcoal headline with one accent word' pattern every other page follows.
  FIX: Put a real quoted line from the pool in the mock with the two answer buttons (the data is already bundled), let the ghost bleed off one edge only, and set the /games/ h1 in charcoal with a single blue word.
  WHERE: site/src/components/GameCard.astro, site/src/pages/games.astro
- [nit] /about/ vs /quizzes/ (desktop 1360, light)
  WHAT: The tiles contradict each other across pages: /about/ says '1 QUIZ', /quizzes/ says '2 QUIZZES' and '1 IN DRAFT'. Both are defensible readings of the data but a reader clicking from one to the other sees the site disagree with itself. Same class of thing: /games/ labels the number 15 'EDITIONS' while /play/ labels the same 15 'SOURCES'.
  FIX: Derive both from one helper that counts published vs draft explicitly, and print '2 quizzes / 1 in draft' on both; pick one word for the 15 and use it everywhere.
  WHERE: site/src/pages/about.astro and site/src/pages/quizzes.astro (tile counts), site/src/pages/games.astro and play/sounds-like-scripture.astro
- [nit] /axis/theology-compass/table/ and /axis/theology-compass/kingdom/ (desktop 1360, light)
  WHAT: In the AxisMap the leader lines are drawn on top of the dots, so the white hairline strikes through the numerals — visible through the '1', '4' and '5' on /table/ and the '3', '4', '5' on /kingdom/. The grey middle-band dots also put dark-grey numerals on mid-grey fill, which is the weakest text on the panel. And on /kingdom/ the panel is badly ragged at desktop: the left column runs to y≈1550 while the middle stops at ≈1100 and the right at ≈1255, leaving a 450px void across two thirds of the panel that the ghost word only partly covers.
  FIX: Draw the leader lines behind the dots (or stop each line at the dot edge), lighten the neutral dot numerals, and at ≥1100px lay the three columns out so they balance — or cap column height and wrap.
  WHERE: site/src/components/AxisMap.astro

## Verdict
No. He would call this a second lazy pass on four pages and a real pass on three. The home page, /quizzes/ and /games/ genuinely read as siblings of the reference; /about/ is close. But /method/'s 89-row changelog, the article body, the quiz-answering screen and /play/ are the same undesigned stacks they were, now wearing a band at the top — and the band is the only thing that changed. Worse, the two pages he judges by are broken: /play/'s PLAY button ships disabled and greyed with no explanation, and the result page — the stated bar — is the one page with no band, no tiles and no section heads, so the site's best page now looks oldest. Dark mode collapses the charcoal-panel device from 13.2:1 to 1.3:1, which flattens every page. Phone specifics: colliding dots and strike-through leader lines on /axis/, a clipped jump bar on /method/, 8.6px tile labels everywhere.

- [broken] /play/sounds-like-scripture/ (phone 390 + desktop, light and dark)
  WHAT: The page's only action is dead on arrival. After full load with JS running I read `document.getElementById('btnPlay').disabled === true` and `opacity: 0.45`, and all three canon chips report `aria-pressed="false"`. In the capture PLAY is a washed grey word on a washed gradient (darkest text pixel #969696 on #C9CBD0, ~1.9:1) and reads as a disabled or broken button. Nothing on screen says a Bible must be chosen first; the gate is a 9.9px letterspaced grey line, "WHICH BIBLE DO YOU READ?", above three identical outline chips with no selected state. Clicking Protestant sets aria-pressed=true and enables PLAY, so the mechanism works — the design simply never tells anyone.
  FIX: Default the canon to Protestant with a visibly pressed chip (filled, not outlined) restored from storage, so PLAY is live on arrival. If the gate must stay, make the requirement the headline of that block and make the disabled state a labelled one ("Pick a Bible to start") rather than a 45%-opacity gradient pill.
  WHERE: site/src/pages/play/sounds-like-scripture.astro and the generated site/src/games/sounds-like-scripture.html (edit demos/sounds-like-scripture/game.src.html and rebuild); #btnPlay, button.chip[aria-pressed]
- [broken] /axis/theology-compass/grace/ (phone 390, light and dark)
  WHAT: Dots in the tradition dot-plot collide. Measured in the live page at 390px: dot 6 and dot 8 are 1px apart, 10 and 13 are 2px apart, 13 and 14 are 1px apart, on the same lane row. At 22px they read as one merged blob, so the numbers that are the whole key to the map become unreadable. Root cause is arithmetic, not luck: `GAP = 8` is 8% of `--run` (315px − 22px dot = 293px) = 23.4px between centres for a 22px dot, leaving 1.4px.
  FIX: Derive the lane gap from the dot size instead of hard-coding 8: require centres at least `dot + 6px` apart, i.e. compute GAP as a percentage at the narrowest supported track (≈9.6 at 390px), or shrink `--dot` to 18px below 26rem and widen the gap to 10.
  WHERE: site/src/components/AxisMap.astro line ~48 (`const GAP = 8`), with `--dot: 22px` in site/src/styles/pages/reference.css `.amap-plot`
- [broken] /axis/theology-compass/gifts/ and /grace/ (phone 390 and desktop 1360, light and dark)
  WHAT: The leader lines are drawn through the dots. `.amap-dot::before` runs from each dot all the way back up to the rail, so a dot in lane 3 paints a 1px line straight across every dot sitting above it in the same column. On Gifts the stem belonging to 11 strikes through 9 and 10 and across the numerals; on Grace 9's stem strikes 8. It looks like the numbers have been crossed out.
  FIX: Draw the stems as a separate layer beneath the dots (a sibling `.amap-stems` with lower z-index, or `z-index: 1` on `.amap-dot` and `z-index: 0` on the stem), and stop each stem at the top of the lane row it belongs to instead of running the full height.
  WHERE: site/src/styles/pages/reference.css lines 99-102 (`.amap-dot::before`)
- [broken] /method/ (phone 390, light and dark)
  WHAT: The "ON THIS PAGE" jump bar is a 1033px-wide row inside a 390px viewport (`.jump` is `overflow-x: auto`, scrollWidth 1033 / clientWidth 390). Chip 03 "WHAT CHANGED" is sliced by the viewport edge and chips 04-08 (Not applied, The test, The numbers, Your answers, The game) are completely invisible, with no fade, no arrow and no hint that the row scrolls. On a phone it reads as a clipped layout bug, and five of the page's eight sections are unreachable from it.
  FIX: Wrap the chips at phone width instead of scrolling them (`flex-wrap: wrap` below 40rem), or keep the scroller and add an edge fade plus a scroll-snap affordance. Also raise the 10.24px chip label.
  WHERE: site/src/components/JumpBar.astro and the `.jump` rule in site/src/styles/pages/prose.css (or reference.css)
- [ugly] /articles/how-to-become-more-grateful/ (desktop 1360, light and dark)
  WHAT: The dark sidebar column is mostly an empty black slab. Its content (spec list plus four TOC chips plus the ALL ARTICLES button) ends about 380px down, and the column then continues as pure flat black for the remaining ~1,300px+ of the article — two full screens of nothing. The Riche sheet reference fills its charcoal column top to bottom with stat blocks; here it is dead weight, and the flat panel shows every pixel of the emptiness.
  FIX: Either make the column sticky at its natural height so it scrolls with the reader, or fill it: the article's cited passages, the topic chips, the related quizzes and the next article all exist in the data. Do not let a dark panel be taller than its content.
  WHERE: site/src/pages/articles/[slug].astro and the article grid rules in site/src/styles/pages/prose.css
- [ugly] every page (phone 390 and desktop, dark)
  WHAT: Dark mode destroys the design language. Measured tokens: light `--dark-fill: #1C1C23` on `--page: #E3E3E3` = 13.2:1, so the charcoal panel is the strongest device on the page; dark `--dark-fill: #343441` on `--page: #131319` = 1.3:1, and `--dark-fill-img` is set to `none` so the starry texture is dropped too. `.band` and `.panel` are both #24242B (1.2:1 vs the page), so the hero sheet does not separate from the body either. The result is that on /axis/, /tradition/, /method/ and /articles/ in dark, every panel, band and dark block is the same near-black and the composition reads as one undifferentiated slab. This is not glare — it is the opposite: nothing is drawn.
  FIX: In dark the charcoal panel has to invert its role: make it the LIGHTEST surface (e.g. #3A3A46 on a #0E0E13 page, ≥2.2:1 against the page) or give it a visible border and a retained texture, and pull `--page` down so `--panel` clears it. Keep a real three-step ladder page < panel < dark-fill in both themes.
  WHERE: site/src/styles/site.css :root[data-theme="dark"] block, lines ~100-119 (`--page`, `--shell`, `--panel`, `--dark-fill`, `--dark-fill-img`)
- [ugly] /method/ (phone 390 and desktop 1360, light and dark)
  WHAT: The changelog is the least designed region on the site and it is enormous: 89 near-identical light cards, each a bold key, a ~11.2px grey summary and a small chevron, running for thousands of pixels on a phone with no rhythm, no numbering, no dates and no visual weight change. It also exposes raw internal keys to readers — `axis:spirit:name`, `axis:spirit:bands`, `axis:spirit:summary` — although the site publishes that axis as "Gifts". In dark the card, panel and page are three near-identical greys, so the rows lose even their card edges, and the keys turn orange, which elsewhere means the right pole of an axis.
  FIX: Group the 89 entries by axis behind a chip filter that is already computed (AXES 42, STATEMENTS 19, TRADITIONS 17, SCORING 4, PAGE COPY 5, OTHER 2), show ~8 with a "show the rest" control, print the human axis name (Gifts) not the internal key, number the entries, and raise the summary to at least 13px. Stop colouring keys with a pole colour.
  WHERE: site/src/pages/method.astro, `.cl-*` rules in site/src/styles/pages/prose.css; the axis key/slug mismatch is `spirit` vs `gifts` (use `groupHref()`/slug per CLAUDE.md)
- [ugly] /tradition/anglican-broad/ and /tradition/pentecostal-.../ (also /, /quizzes/, /axis/, /about/, /games/, /method/) (phone 390, light and dark)
  WHAT: The stat tiles are not a grid and the labels are unreadable. `.tile span` is `font-size: .54rem` (8.64px measured) with `letter-spacing: .13em` and `white-space: nowrap`, inside `grid-template-columns: repeat(auto-fit, minmax(4.4rem, 1fr))`. On the tradition hero "OTHERS COMPARED" therefore forces its own column wide and fills it wall to wall with about 2px of padding either side, while "AXES" sits in a narrow tile with huge padding — three tiles, three different widths, one of them cramped. 8.64px letterspaced caps is below any readable floor on a phone.
  FIX: Raise `.tile span` to ~11px on phone, drop `white-space: nowrap` and allow a two-line label, and give `.tiles` equal columns (`repeat(2, 1fr)` at 390 for four tiles, `repeat(3, 1fr)` for three) so every tile is the same width. Shorten "OTHERS COMPARED" to "OTHERS" in the data.
  WHERE: site/src/styles/kit.css lines 126-144 (`.tile span`, `.tiles`) and line 392; hero tiles set in site/src/pages/tradition/[slug].astro
- [ugly] /q/theology-compass/ (the answering screen, after Start) (phone 390, light)
  WHAT: The screen a reader spends three minutes on got the least design on the site. Five identical white pills with five identical empty grey circles, no mapping at all from "Strongly disagree" to "Strongly agree" — no gradient, no size step, no blue-to-orange hint, nothing that distinguishes the ends from the middle. Above it an 18-pip progress bar in which nothing is filled at statement 1, so it reads as a broken control. The full site nav sits above the question and the entire dark footer (four link columns) sits below it, so the quiz is never given a focused frame. The statement card's wash is a peach blob confined to its lower-left corner with the top half left plain grey.
  FIX: Give the five options a real scale: tint the two disagree options toward blue, the two agree options toward orange and leave "Unsure / neither" neutral with the hatched treatment used elsewhere for the no-claim band, and step the circle weight. Fill pip 1 as soon as the question is shown. Suppress the nav and the footer while a run is in progress. Run the card's wash across the whole card.
  WHERE: site/src/pages/q/[quiz].astro and site/src/styles/pages/quiz.css
- [ugly] /tradition/anglican-broad/ (desktop 1360, light)
  WHAT: The "nearest neighbours" bars are two different components on one list. The top "87% MATCH · CLOSEST" bar is painted with a blue-orange-blue gradient plus a floral texture overlay and terminates in a hard-edged blue block with a visible vertical seam at its right end; the five bars below have neither the texture nor the seam and are washed pastel. It reads like a rendering artefact. Worse, a match percentage is not an axis, so painting it in the two pole colours breaks the site's own rule that blue means the left pole and orange the right and neither is good.
  FIX: Use one neutral ink for all six bars and mark "closest" with the dark chip the kit already has, not with a different fill. Drop the floral overlay from the first row or apply it to all. Fix the hard seam at the end of the fill.
  WHERE: site/src/pages/tradition/[slug].astro and the neighbour-bar rules in site/src/styles/pages/reference.css
- [ugly] /c/theology-compass/01IBS6.01IBS6/ (phone 390, light)
  WHAT: The six-row rhythm breaks on two rows. Four rows print the axis name on the left and the readout right-aligned on the same line; AUTHORITY's readout ("same band: rooted in Scripture and tradition") is too long and drops to its own line below the name, shifting that rail down and leaving the name alone on a line; WORSHIP's readout starts immediately after the word WORSHIP with no separation, so the two collide. The result is that the one thing the page exists to show — six rows that scan as one set of rails — does not scan.
  FIX: Put the readout on its own line under the axis name on every row at phone width (a consistent two-line head), or truncate the band phrase to a fixed short form. Never let the wrap decide the layout row by row.
  WHERE: site/src/pages/c/[quiz]/[codes].astro and the `.rail-head` / `.rail-read` rules in site/src/styles/pages/reference.css
- [weak] /r/theology-compass/01IBS6/ (phone 390 and desktop, light and dark)
  WHAT: The page the whole pass was supposed to match is now the only page that does not speak the new language. Queried live: `document.querySelector('.band')` is null, `.tiles` is null, `.sect-head/.sect-title` count is 0. Its section titles ("Loose neighbours, not matches", "Where these differences come from") are plain upright serif with no kicker, no lowercase italic display, no gradient rule; its header is a bare heading on the page background with no sheet, no eyebrow chip and no ghost word. Put /r/ next to /quizzes/ and the result page looks like the old site.
  FIX: Give the result page the band header and `SectionHead` shell (eyebrow chip, ghost word, tiles with the real numbers it already has: axes, statements, traditions compared) without touching the instruments inside it. That is a shell change, not a redesign of the rails.
  WHERE: site/src/pages/r/[quiz]/[code].astro; `.result-head`, `.result-panel` currently used instead of `.band` / `.sect-head`
- [weak] /q/seven-deadly-sins/ (desktop 1360 and phone 390, light)
  WHAT: Two problems. (1) "what you will see" is seven small outline chips on one line plus two lines of caption inside a full-width dark panel, leaving roughly two thirds of it empty at 1360 and a ragged 4+3 wrap at 390 — beside the Compass's six full rails with both pole names and eleven pips it looks like a placeholder. (2) The hero carries no draft mark at all; the "Unaudited draft" warning sits about 2,500px down the phone page, below "how to answer", although the /quizzes/ card marks the same quiz DRAFT on its face.
  FIX: Draw the seven categories as a real ranking instrument — seven named bars of equal length with the strongest-wins rule stated — so the unipolar shape reads as deliberately different from the bipolar one. Put the draft badge in the hero chip beside "14 STATEMENTS · ABOUT 2 MINUTES".
  WHERE: site/src/pages/q/[quiz].astro (the unipolar branch of the preview) and site/src/components/QuizPreview.astro
- [weak] /axis/theology-compass/gifts/ (phone 390, light and dark)
  WHAT: The pole subtitle wraps as "Continuationist ↔" on line one and "Cessationist" on line two, orphaning the arrow at the end of the first line where it points at nothing. On /grace/ the same line fits and reads correctly, so the page looks broken only on the longer axes — exactly the inconsistency that makes a design feel unconsidered.
  FIX: Keep the arrow glued to the second pole (`&nbsp;` before it or a nowrap span around "↔ Cessationist"), or stack the poles deliberately: left pole, a centred arrow on its own line, right pole.
  WHERE: site/src/pages/axis/[quiz]/[axis].astro, the band subtitle that prints `{group.left} ↔ {group.right}`
- [weak] /articles/how-to-become-more-grateful/ (phone 390 and desktop 1360, light and dark)
  WHAT: Four screens of unbroken grey prose in a single panel. No pull quote, no scripture panel, no dark break, no marginal note, no tiles — every device the references use, and every device this site already owns, is absent from its longest reading surface. "Four practices" is a genuine four-item sequence (Name particulars / Attach it to asking / Say it out loud / Practise it when it is untrue to your mood) and is rendered as four bold run-in leads with no numbers, so the structure is invisible while scrolling.
  FIX: Number the four practices with the numbered markers the kit has, lift one sentence per section into a display pull quote, and set the quoted verses (1 Thess 5:18, Rom 1:21, Luke 17:11-19, Ps 103, Phil 4:6) as a passage panel in the site's own passage styling rather than inline. The brief allows new components.
  WHERE: site/src/pages/articles/[slug].astro, site/src/styles/pages/prose.css, content in site/src/content/articles/
- [weak] /axis/theology-compass/grace/ ("the two cases") (phone 390 and desktop, light and dark)
  WHAT: The two mirrored pole panels are 13-line and 11-line slabs of body copy with nothing inside them but a coloured title and a thin edge bar. The disagreement is supposed to read as a disagreement; at 390 the two panels are simply two long paragraphs stacked vertically, so the mirroring that justifies the device is invisible on the viewport that matters most.
  FIX: Give each panel internal structure from data that already exists: the pole name, its one-line claim as a display lead, then the body, then that pole's `readMore` as chips. At phone width label the pair explicitly (LEFT POLE / RIGHT POLE tabs exist as chips already) so the mirror survives stacking.
  WHERE: site/src/pages/axis/[quiz]/[axis].astro, `.pole-panel` rules in site/src/styles/pages/reference.css
- [weak] /tradition/anglican-broad/ (phone 390 and desktop 1360, light and dark)
  WHAT: The rail readouts contradict the rails. Measured from the DOM: Table prints "90% toward Sacramental" but `--pos` is 8.33 (a lean of 83%); Kingdom prints "56% toward One people" with `--pos` 25 (a lean of 50%); Worship prints "90%" with `--pos` 8.33. The readout is computed from the raw position (5, 22, 5) and the knob from the outward-snapped reachable step. On a site whose entire moat is that the numbers are checkable, a screenshot showing a knob at half-way beside the words "56%" is the kind of thing this audience posts.
  FIX: On the tradition page draw the knob at the raw coordinate (as AxisMap already does deliberately, and for the reason its own comment gives) rather than at the quiz's snapped step, or state the snapped figure in the readout. One number per rail.
  WHERE: site/src/pages/tradition/[slug].astro, the `--pos` passed to the rail; compare site/src/components/AxisMap.astro lines 14-20
- [weak] /games/ vs /articles/, /about/, /axis/*, /method/ (phone 390 and desktop, light)
  WHAT: Hero title colouring is arbitrary. "Games" is set entirely in blue; "Articles", "About", "Grace", "Gifts", "Anglican (broad)" are entirely dark; "How this was made" and "See where you actually stand." colour one word. There is no rule a reader can infer, so the accent stops meaning anything, and blue is supposed to be reserved for the left pole of an axis.
  FIX: Pick one rule and hold it — accent the last word of a multi-word title, never a whole single-word title — and keep pole blue off page titles that have no axis.
  WHERE: site/src/pages/games.astro, articles/index.astro, about.astro, axis/[quiz]/[axis].astro (the `title` passed to Hero/band)
- [nit] /about/ vs /quizzes/ (phone 390, light)
  WHAT: The two pages disagree about how many quizzes exist. /about/ tiles read "1 QUIZ · 1 GAME · 3 ARTICLES · 18 TRADITIONS"; /quizzes/ tiles read "2 QUIZZES · 32 STATEMENTS · 18 TRADITIONS · 1 IN DRAFT". Both are presumably true under different filters, but nothing on either page says so, and rule 7 of the brief is that tiles carry real counts.
  FIX: Label the /about/ tile "1 AUDITED QUIZ" (or count both and add an "IN DRAFT" tile as /quizzes/ does) so the two pages cannot be screenshotted side by side as a contradiction.
  WHERE: site/src/pages/about.astro tile list vs site/src/pages/quizzes.astro
- [nit] every page (site footer) (phone 390, light and dark)
  WHAT: The footer ghost word is cropped to a horizontal slice: only the x-height band of "wiser wal" survives, sliced flat at the baseline by the dark panel's bottom edge and cut mid-letter at the right. In the references (ref-riche-sheet, ref-epitaph) the cropped display word is always still legible as a word; here it reads as a rendering error rather than a device.
  FIX: Raise the word so at least its full x-height plus descenders sit inside the panel, or move it to bleed off one edge only, not two.
  WHERE: `.foot-ghost` in site/src/styles/site.css (measured left -4 / right 442 at a 390 viewport)
- [nit] every page (chrome and reference lists) (phone 390, light and dark)
  WHAT: Tap targets and label sizes sit under the brief's own floor. Measured at 390: header nav links 37px tall, footer links 36px, brand 24px, the passage disclosure pills on /axis/ 34px (eight of them), the /method/ jump chips 20px before their padding resolves. Reference-list text runs small too: `.amap-chip` tradition names 9.28px, `.amap-group-name` 9.92px, `.sect-kicker` 9.92px, `.cl-meta` 11.2px. Rule 1 of the brief says 44px and this is the phone-first pass.
  FIX: Set a 44px min-height on nav, footer and pill controls (padding, not font size), and raise the reference-list labels to 11-12px. The tracking already makes them hard; the size makes them impossible.
  WHERE: site/src/styles/site.css (`.site-nav a`, `.site-foot a`), `.pill` in site/src/styles/pages/reference.css line ~129 (`.amap-chip { font-size: .58rem }`)
